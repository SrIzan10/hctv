services:
  psql:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      # my condolences
      POSTGRES_PASSWORD: skbiditoilet
    volumes:
      - ./psql:/var/lib/postgresql/data
    ports:
      - 5555:5432